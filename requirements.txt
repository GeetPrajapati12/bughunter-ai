selenium>=4.21.0
playwright>=1.44.0
anthropic>=0.27.0
beautifulsoup4>=4.12.3
lxml>=5.3.0
requests>=2.32.3
Pillow>=10.4.0
jinja2>=3.1.4
python-dotenv>=1.0.1
rich>=13.7.1
loguru>=0.7.2
pydantic>=2.7.3
aiohttp>=3.9.5
tqdm>=4.66.4

# weasyprint is optional (PDF report generation) and notoriously hard to
# install on Windows due to GTK system dependencies. Install separately
# only if you need PDF reports — HTML reports work without it.
# weasyprint>=62.3
